<Page x:Class="WpfApp.Pages.EnterDataPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="EnterDataPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>
        <DataGrid Name="dtData" CanUserAddRows="False" CanUserDeleteRows="False" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn IsReadOnly="True" Header="Вид продукции" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Каллорийность" />
                <DataGridTextColumn Header="Вес"/>
                <DataGridTextColumn Header= "Максимальное количество"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Style="{StaticResource ButtonTemplate}" Width="200" Height="50" Grid.Row="1" Name="btnSolve" Content="Решить"/>
    </Grid>
</Page>
